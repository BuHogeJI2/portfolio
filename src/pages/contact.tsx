import React from 'react';
import { ContactForm, Layout } from '../modules';

export default function ContactPage(): React.ReactElement {
  return (
    <Layout>
      <ContactForm />
    </Layout>
  );
}
