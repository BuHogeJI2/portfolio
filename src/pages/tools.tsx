import React from 'react';
import { Layout, ToolsContent } from '../modules';

export default function ToolsPage(): React.ReactElement {
  return (
    <Layout>
      <ToolsContent />
    </Layout>
  );
}
