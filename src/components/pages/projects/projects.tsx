import { ExpandableCards } from '@/components/base/expandable-cards/expandable-cards';
import { ReactElement } from 'react';

export function Projects(): ReactElement {
  return <ExpandableCards cards={[]} />;
}
